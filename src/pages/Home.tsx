import React, { FC } from 'react';

import SearchCondition from 'components/SearchCondition'

const Home: FC = () => {
  return (
    <main>
      <SearchCondition />
    </main>
  )
};

export default Home;
